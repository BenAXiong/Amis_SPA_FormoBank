# Privacy Notes

Normal translation requests are not stored by this app.

Corrections and annotations are stored only when a user submits feedback and explicitly consents to review/storage.

Submitted corrections must not be used for training unless `consent_for_training` is true.
